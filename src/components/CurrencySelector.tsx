'use client';

import React from 'react';

/**
 * CurrencySelector is disabled as the application is now locked to INR only.
 */
export const CurrencySelector: React.FC<any> = () => {
  return null;
};
